import { TwinWorkspace } from '@/features/twin/components/TwinWorkspace';

export default function TwinPage() {
  return (
    <div className="container mx-auto py-6 px-4">
      <TwinWorkspace />
    </div>
  );
}
